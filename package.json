{
  "name": "grantmind",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "npx ts-node deploy/deployScript.ts",
    "test": "gltest --network studionet"
  },
  "devDependencies": {
    "genlayer-js": "^0.7.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
